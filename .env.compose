APP_NAME=yy
HOST=yy.jeremyweeks.com
PORT=3000

# Multi Environment Variables
# ----------------------------------------------
# VERSION=v1
# STACK=test

# Traefik Dynamic Path Configuration
# ----------------------------------------------
# DYNAMIC_PATHS=TRUE
# TRAEFIK_PATH_RULE=&& PathPrefix(`/${APP_NAME}/${VERSION}`)
# TRAEFIK_MIDDLEWARES=${APP_NAME}-${STACK}-strip-${VERSION}
# TRAEFIK_STRIP_PREFIXES=/${APP_NAME}/${VERSION}